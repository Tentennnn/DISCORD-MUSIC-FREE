const Discord = require("discord.js");
const config = require("../config.js");
module.exports = {
    help: {
        name: "help",
        aliases: ["h"]
    },
    run: async (client, message, args) => {
        
    }
};